/**
 * The published surface of the Site Notes module: the only package the core and other
 * modules may import from (see {@code ModuleBoundaryTest}). Put domain events, read-only
 * facades and shared value types here; everything else in the module stays internal.
 */
package org.tornotron.echno_backend.modules.sitenotes.api;
