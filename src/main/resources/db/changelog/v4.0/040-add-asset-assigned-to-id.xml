<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="040-add-asset-assigned-to-id" author="echno">
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists tableName="asset" columnName="assigned_to_id"/>
            </not>
        </preConditions>

        <comment>
            Store the assignee's employee id alongside the existing free-text assigned_to name so the
            asset register can be filtered by assignee. Nullable: the name may not resolve to an
            employee, and older rows are backfilled best-effort in a following changeset.
        </comment>

        <addColumn tableName="asset">
            <column name="assigned_to_id" type="BIGINT"/>
        </addColumn>

        <createIndex tableName="asset" indexName="idx_asset_assigned_to_id">
            <column name="assigned_to_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
